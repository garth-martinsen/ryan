Plan to get a functioning adc_client with sub objects: volts_adc, amps_adc that will measure and report to the svr.
Note: Renamed modules and classes: adc.py->volts_adc.py VoltsAdc->VoltsAdc, ah_meter.py -> amps_adc.py AhMeter->AmpsAdc, ...
1. Create dirs: up (micropython ), up/lib, tests_up, tests_up/unit, tests_up/integration, tests_up/end_to_end??,
2. In the up dir add files __init__.py, adc__client.py, volts_adc.py, adc_cfg.py, amps_adc.py, gps.py
3. In the up/lib dir add files: ads1x15.py, ...
4. In the tests_up/unit dir add files: test__client, test_volts_adc.py, test_amps_adc.py  
5. Start with a simple adc_client.py , then wrap it in a class and integration test it.
6. Add code in adc_client.py to fetch app_config from svr/db. Integration test it.
7. Add code in adc_client.py to instantiate adc= VoltsAdc(APP_ID, VERSION) . Test it.
8. Add code in adc_client.py to instantiate amps_adc = AmpsAdc(....). Test it.
9. Add code in volts_adc.py to measure(...). Integration Test it. Unit test it.
10. Add factory code in volts_adc.py to provide a template for ADC_TO_SVR_VOLTAGE_REPORT msg. It will be populated with data from a2d fields and rqd fields.
11. Add code in volts_adc.py to build_measurement_report(...). Unit test it. Integration test it. 
12. Add code in amps_adc.py to process current measurements. Unit and Integration test it.
13. Add code in amps_adc.py to contribute to the voltage_report. Unit and Integration test it.
14. Add code in gps.py to receive and format a msg to send GPS data to the svr for persistance. Test it unit and integration.



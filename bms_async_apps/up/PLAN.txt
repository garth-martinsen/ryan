Plan to get a functioning adc_asyncio_client with sub objects: adc, ah_meter that will measure and report to the svr.
1. Create dirs: up, up/lib, tests_up, tests_up/unit, tests_up/integration, tests_up/end_to_end,
2. In the up dir add files __init__.py, adc_asyncio_client.py, adc.py, adc_cfg.py, ah_meter.py, gps.py
3. In the up/lib dir add files: ads1x15.py, ...
4. In the tests_up/unit dir add files: test_asyncio_client, test_adc.py, test_ah_meter.py  
5. Start with a simple adc_asyncio_client.py , then wrap it in a class and integration test it.
6. Add code in adc_asyncio_client.py to fetch app_config from svr/db. Integration test it.
7. Add code in adc_asyncio_client.py to instantiate adc= ADC(APP_ID, VERSION) . Test it.
8. Add code in adc_asyncio_client.py to instantiate ah_meter = AhMeter(....). Test it.
9. Add code in adc.py to measure(...). Integration Test it. Unit test it.
10. Add code in adc.py to deepcopy the template for ADC_TO_SVR_VOLTAGE_REPORT msg. It will be populated with data from a2d fields and rqd fields.
11. Add code in adc.py to build_measurement_report(...). Unit test it. Integration test it. 
12. Add code in ah_meter.py to process current measurements. Unit and Integration test it.
13. Add code in ah_meter.py to add to the voltage_report. Unit and Integration test it.
14. Add code in gps.py to receive and format a msg to send GPS data to the svr for persistance. Test it unit and integration.


